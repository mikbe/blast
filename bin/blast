#!/usr/bin/env ruby
$:.unshift File.expand_path(File.join(File.dirname(__FILE__) + "/../lib"))
require 'blast'

# We pass in ARGV so we can build tests more easily since ARVG is read-only
blast = Blast::Setup.new
blast.parse_command_line(ARGV)
